# Built site — do not edit here

This branch contains only the built website (the contents of `dist/`).
It exists so Hostinger can deploy straight from GitHub.

Never edit these files: they are overwritten by every build.
The real source lives on the `main` branch.
